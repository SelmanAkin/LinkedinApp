<!DOCTYPE html>
<html>
  <head>
    <title>LinkedInApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">
    <%= javascript_importmap_tags %>
  </head>

  <body style="margin: 0; padding: 0;">
    <div style="width: 100%; max-width: none; margin: 0; padding: 0;">
      <table style="width: 100%; table-layout: fixed; border-collapse: collapse; margin: 0;" border="0" cellspacing="0" cellpadding="0">
        <tr style="background-color: #0056b3; color: white;">
          <td style="padding: 14px 20px;">
            <%= link_to "LinkedInApp", root_path, style: "color: white; font-weight: bold; text-decoration: none;" %>
          </td>
          <td style="text-align: right; padding: 14px 20px;">
            <% if user_signed_in? && current_user.admin? %>
              <%= link_to "Admin Paneli", admin_users_path, style: "color: white; margin-right: 10px; text-decoration: none;" %>
            <% end %>

            <% if user_signed_in? %>
              <%= link_to "Profilim", user_path(current_user), style: "color: white; margin-right: 10px; text-decoration: none;" %>
              <%= button_to "Çıkış Yap", destroy_user_session_path, method: :delete, form: {style: "display:inline"} %>
            <% else %>
              <%= link_to "Giriş Yap", new_user_session_path, style: "color: white; margin-right: 10px; text-decoration: none;" %>
              <%= link_to "Kayıt Ol", new_user_registration_path, style: "color: white; text-decoration: none;" %>
            <% end %>
          </td>
        </tr>
      </table>
    </div>

    <% if notice.present? %>
      <p><%= notice %></p>
    <% end %>

    <% if alert.present? %>
      <p><%= alert %></p>
    <% end %>

    <%= yield %>
  </body>
</html>
